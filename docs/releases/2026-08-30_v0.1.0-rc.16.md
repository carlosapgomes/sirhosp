# Release Evidence Pack — v0.1.0-rc.16 (2026-08-30)

## Publicação imutável

- Tag `v0.1.0-rc.16` → commit `c652e652e2d87f58b184bd9d2b4916185e634f76`
  (`docs(release): prepare v0.1.0-rc.16 upgrade runbook`), pushed; contém
  todos os commits de implementação HTEFS-S1..S5 (`48f1424`, `7dcbc7c`,
  `8854761`, `baf4ff8`, `7cf7e8a`) e o arquivamento (`af73d04`) como
  ancestrais.
- Workflow `Publish Release Image` (run `33338821891`): `Validate exact
  release tag` 5m50s + `Publish immutable production release` 3m2s, ambos
  sucesso (qualidade integral executada dentro da validação; anotações
  apenas de deprecação de Node 20 nas actions, não-bloqueantes).
- Release GitHub: `isDraft=false`, `isPrerelease=true`, `immutable=true`.
- Assets: `compose.hospital.yml` (SHA-256
  `0ee94843609e21715c49a22e27e82ddfde3caeaa99b49ccbb03571690d6ae923`,
  inalterado desde a RC10) e `v0.1.0-rc.16-upgrade.md` (SHA-256
  `017756ab2d6de66d0a39b6743987659c7b1aa79b95bcb5b0392e8eeedca3c1a7`).
- Imagem GHCR `ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.16`:
  digest `sha256:26a2dba1c184a752392e52f0e0359affbfaa51eeaee6fc393fa9d1898f02d504`;
  label `org.opencontainers.image.revision` =
  `c652e652e2d87f58b184bd9d2b4916185e634f76` (igual ao commit da tag).

## Gates pré-release (estação, tag commit)

Executados na estação durante a verificação independente dos slices
(re-executados, não apenas lidos dos relatórios):

- `check`: no issues (0 silenced); `lint`: All checks passed!;
  `typecheck`: 352 arquivos sem issues; `markdown-lint.sh`: 0 erros;
- `unit`: **3104 passed** (baseline rc.15 = 3019; +85 = 6 S1, 26 S2, 14 S3,
  23 S4, 16 S5); `integration`: **391 passed** (constante);
- RED de cada slice reproduzido independentemente antes do GREEN;
- nenhum change OpenSpec ativo na tag (HTEFS arquivado em `af73d04`,
  specs promovidas: `evolution-extraction-coverage` criada +4; outras três
  +9 requisitos).

## Preflight e baseline pré-rollout (§6.1.3/§6.3.2)

Servidor `eon`, `/srv/apps/prisma`, shell root:

- `.env` modo 600 com `SIRHOSP_VERSION=v0.1.0-rc.15`; disco 396G livres;
- Compose RC15 `config --quiet` OK; `db` healthy (12 dias); dez réplicas
  `persistent_worker` rc.15 up; `pg_isready` OK; portal `{"ok": true,
  "service": "sirhosp"}`; rede `hospital_edge` somente inspecionada;
- health check 24h (baseline): `healthy=true`,
  `empty_success=0 missing_full_sync=0 duplicate_demographics=0`,
  queue `active=1124 oldest_age_minutes=4`,
  full_sync `terminal=10310 succeeded=10127 failed=183
  failure_percent=1.8` (`invalid_payload=24, timeout=159`), frescor
  presente (movement 4min / admission 0min / event 25min);
- caracterização 7d (baseline): coorte `patients=20 failed_runs=507
  attempts_median=11 attempts_max=112` (`timeout=452,
  invalid_payload=55`) — janela ainda majoritariamente pré-RC15,
  coerente com o regime pós-fail-fast; `evolution_extraction`
  median 42.7s / p90 56.6s.

## Drenagem, backup e swap

- `running_census=0`; parados `census_orchestrator` + `web`;
- drenagem da fila com workers ativos (~55 min):
  `1124 → 1073 → 999 → 905 → 706 → 342 → 24 → 0`
  (composição do ciclo: admissions 292 / demographics 293 / full_sync 293;
  nenhum run diferido no período);
- parados `persistent_worker` (10 réplicas) + `summary_worker`;
- backup (somente DB ativo):
  `/srv/apps/prisma/backups/sirhosp-20260830T233502Z.dump`
  (567.659.111 bytes, modo 600; diretório 700; SHA-256 em `.sha256`
  verificado com `SUCESSO`);
- swap: `compose.hospital.yml.rc15` preservado (rollback);
  `compose.hospital.yml.rc16` validado por checksum (`SUCESSO`) e
  `config --quiet`; `.env` → `SIRHOSP_VERSION=v0.1.0-rc.16` (modo 600).

## Migrations (primeira mudança de schema desde a RC15)

- 1ª execução: `Applying ingestion.0006_evolutionextractioncoverage... OK`
  (única pendente);
- 2ª execução: `No migrations to apply.` (idempotente);
- `SELECT count(*) FROM ingestion_evolutionextractioncoverage` → `0`
  imediatamente pós-migrate (tabela nova vazia, sem backfill).

## Topologia pós-swap

- `web` up; portal `{"ok": true, "service": "sirhosp"}`; `/beds/` → 302;
- `persistent_worker` × 10 + `summary_worker` + `census_orchestrator` up;
- imagem em execução = `ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.16`
  (exata), revisão do label = commit da tag;
- `manage.py check`: no issues (0 silenced);
- erros estruturais (10 min, todos os serviços): `structural_errors=0`.

## Health checks pós-deploy

- janela 2h: `healthy=true`, `empty_success=0 missing_full_sync=0
  duplicate_demographics=0`, queue `active=0`,
  full_sync `terminal=1170 succeeded=1156 failed=14
  failure_percent=1.2` (`timeout=14`), `events_created=6`;
- janela 24h: `healthy=true`, `empty_success=0 missing_full_sync=0
  duplicate_demographics=0`, queue `active=0`,
  full_sync `terminal=10511 succeeded=10325 failed=186
  failure_percent=1.8` (`invalid_payload=24, timeout=162`).

## Observação da correção (§6.3.2) — primeiras horas

Novos sinais RC16 (contagens agregadas via `manage.py shell`):

- T+0 (pós-topologia): `coverage_rows=0`, `deferred_queued=0`,
  `chunk_commit_rows=0` (sistema ocioso aguardando ciclo);
- T+~10 min: novo ciclo de censo enfileirado (1161); **guard vivo**:
  `deferred_queued=1` (primeiro `full_sync` automático nascido diferido
  após falha terminal recente);
- T+~27 min: prazo do defer venceu sozinho (`deferred_queued=0`) — teto
  fixo sem extensão, conforme D5;
- T+~55 min (primeiros full_syncs alvo completando):
  `coverage_rows=63`, `coverage_event_total=3070`,
  `coverage_zero_rows=1` (chunk explicitamente vazio confirmado como
  cobertura — semântica do ledger), `chunk_commit_rows=63`
  (`failed=0` — uma métrica por chunk confirmado), `deferred_queued=1`
  (novo defer do ciclo), queue 539 / running 9;
- leitura da coorte (§6.2.1) fica para as horas/dias seguintes: espera-se
  `attempts_median/max` estável (retry intra-run intacto) e queda de
  re-extração conforme o ledger cobre internações fechadas.

## Canário §6.1.2 — timer systemd (instalado na RC15)

- `sirhosp-ingestion-health.timer`: ativo (hourly);
- execução 20:00 local: **falha esperada e explicada** —
  `service "web" is not running` (web parado para a drenagem do upgrade);
  registrada como janela de manutenção, não incidente;
- execução 21:00 local: `Deactivated successfully` / `Finished` com saída
  agregada do health check no journald — canário verde pós-deploy.

## Decisões do operador registradas

- Worker clássico permanece fora da topologia de produção (decisão RC15);
  este change não o altera (somente leitura em todos os slices).
- Nenhum ajuste do guard nesta release: o valor fixo de 60 min segue o
  design D5; reavaliar somente com dados de observação (deferimentos por
  ciclo, tempo até reprocesso).
- Migração do Postgres 16 → 18.4-trixie em estudo paralelo (blueprint de
  infraestrutura); fora do escopo desta release e sem interferência no
  deploy (inspeção de imagens apenas).

## Rollback preservado

- `compose.hospital.yml.rc15` preservado no servidor; rollback =
  redeploy RC15 (procedimento no runbook);
- tabela `ingestion_evolutionextractioncoverage` fica como órfã inócua
  sob a RC15 (sem reversão de migration e sem restauração de backup para
  rollback funcional);
- backup `sirhosp-20260830T233502Z.dump` retido para incidentes que exijam
  restauração coordenada.

## Checklist final

- [x] tag exata nova, release e imagem publicadas imutáveis; assets e
      digest verificados na estação e no servidor;
- [x] baseline pré-rollout registrado (health 24h + caracterização 7d);
- [x] drenagem completa (zero queued/running/batches) antes de parar os
      workers; nenhum run mutado, descartado ou reenfileirado manualmente;
- [x] backup obrigatório com checksum verificado (schema change);
- [x] `Applying ingestion.0006... OK` + idempotência + tabela vazia;
- [x] topologia íntegra: 10 workers, imagem exata, revisão = commit,
      `manage.py check` limpo, 0 erros estruturais;
- [x] health checks 2h e 24h com `empty_success=0`;
- [x] primeiros sinais RC16 observados: guard diferindo/vencendo, ledger
      crescendo (63 chunks / 3070 eventos / 1 cobertura vazia),
      telemetria `evolution_chunk_commit` 1:1 com commits, 0 falhas de
      commit;
- [x] canário §6.1.2 ativo; execução pós-deploy verde;
- [x] nenhum `--apply`, recovery, mutação de runs, reversão de migration
      ou comando de catálogo/ativação executado.
