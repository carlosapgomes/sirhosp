# Release Evidence Pack — v0.1.0-rc.15 (2026-08-29)

Change: `fix-fullsync-failure-exhaustion` (arquivado como
`2026-08-29-fix-fullsync-failure-exhaustion`; spec
`fullsync-failure-exhaustion-fix`).
Upgrade runbook: `docs/releases/v0.1.0-rc.15-upgrade.md` (asset imutável da
release).
Rollout: 2026-08-29 ~22:31–22:33 UTC, `root@eon:/srv/apps/prisma`, conforme
runbook. Operação por agregados somente; `.env`/Compose interpolado nunca
impressos; nenhum `--apply`/recovery/mutação de runs executado.

## Publicação imutável

- Tag `v0.1.0-rc.15` → commit `682efb1` (runbook da release; ancestrais:
  FX-S1 `801a7e3`, FX-S2 `157d189`, FX-S3 `15cae88`, FX-S4 `800e13c`,
  arquivamentos `7d0cae6`/`6cd85e4`, evidence RC14 `11be909`).
- Workflow `publish-release-image.yml` run `33276871767` → success
  (quality-gate completo no tag commit).
- Release: `draft=false`, `prerelease=true`, immutable releases exigido pelo
  workflow (passou). Assets: `compose.hospital.yml` (SHA-256
  `0ee94843609e21715c49a22e27e82ddfde3caeaa99b49ccbb03571690d6ae923`,
  inalterado desde RC10) e `v0.1.0-rc.15-upgrade.md` (SHA-256
  `d94531ebdfa551ad9ff10c55c99c245191a6f6804993d575dfc61a14ac8ca9ee`,
  conferido no servidor).
- Imagem `ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.15`:
  digest `sha256:277b82726d4b75cf55eb6c09f5345aeda817c9a362f598ac30954f81f4a28d88`
  (registry = pull local);
  OCI revision `682efb1b588a72ad02fbbd535e4b9c79b7007b83` = commit da tag
  (label confirmado também na imagem local do servidor).

## Gates pré-release (estação, tag commit)

`quality-gate` exit 0 — check, unit **3019 passed** (2991 pré-change +42:
FX-S1 +14, FX-S2 +18, FX-S3 +10; zero failures/errors), lint, typecheck
(347 files); integration **391 passed**; `openspec validate --strict`
valid; markdown-lint 0 erros (633 arquivos). Migrations no intervalo
`v0.1.0-rc.14..v0.1.0-rc.15`: zero; dependências: zero; catálogo v5
`c84af977…` inalterado.

## Preflight e baseline pré-rollout (§6.1.3/§6.3.2)

- Estado: `.env` modo 600, versão rc.14, Compose config OK, `pg_isready` OK,
  portal `{"ok": true}`, 10 `persistent_worker`, disco 10%.
- Health (24h, 22:14 UTC): `healthy=true`, `empty_success=0`,
  `duplicate_demographics=0`, fila `active=810`,
  full_sync `terminal=9102 succeeded=8863 failed=239` →
  **`failure_percent=2.6`**, reasons `timeout=228, invalid_payload=11`.
- Caracterização (7d, 22:16 UTC): coorte
  **`patients=24 failed_runs=489 attempts_median=16.5 attempts_max=112`**,
  reasons `timeout=423, invalid_payload=66`, contraste
  `invalid_payload=589, timeout=870`,
  `terminal_failing_stages: evolution_extraction=489` (100%),
  stage `evolution_extraction` `median=43.2s p90=120.0s`
  (o teto fixo em ação). Coerente com o one-shot rc.14
  (23 pacientes/453 runs/mediana 15/máx 112).
- Metadados: catálogo v5 `c84af977…`; `baseline_24h` admissions
  `succeeded=9074, succeeded_seen_zero=0, failed=241`.

## Drenagem, backup e swap

- `running_census=0` no início; `census_orchestrator`/`web` parados;
  drenagem monitorada por agregados: 513+6 (22:37) → 169+9 (22:47) →
  1+1 (22:54) → **todos zero** (22:58 UTC; inclui `batches_running=0`,
  summaries/pipeline zero). Sem intervenção manual em runs.
- Backup: `backups/sirhosp-20260829T223006Z.dump`, 554.075.788 bytes,
  custom format, modo 600 (dir 700), SHA-256 verificado
  (`sirhosp-20260829T223006Z.dump.sha256`).
- Swap: `compose.hospital.yml.rc14` preservado (rollback), asset rc15
  validado por SHA-256 e `config --quiet`, `.env` → `v0.1.0-rc.15` (modo
  600, nunca impresso), pull digest idêntico ao registry.
- Migrations ×2: **`No migrations to apply`** (idempotente, conforme
  esperado — zero migrations no intervalo).

## Topologia pós-swap

`web` healthy (health `{"ok": true}`, `/beds/` → 302), 10
`persistent_worker`, `summary_worker`, `census_orchestrator`, `db` (11 dias).
Imagem exata em execução confirmada (`Config.Image` =
`ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.15`); `manage.py check` sem issues;
**`structural_errors=0`** (logs 10m). Primeiro ciclo natural retomou
(~1175 runs enfileirados às 22:48, drenando normalmente).

Nota operacional: o primeiro `up -d` após o swap usou flag `-q` inválida
para `up` (erro imediato de CLI, sem efeito); corrigido na sequência com o
comando correto. Sem downtime adicional relevante (web já estava parado
para o swap).

## Health checks pós-deploy

- **2h** (00:38 UTC): exit 0, `healthy=true`, `empty_success=0`,
  `duplicate_demographics=0`, full_sync `terminal=592 succeeded=585
  failed=7` → **`failure_percent=1.2`**, reasons `timeout=7` (zero
  `invalid_payload` em full_sync na janela).
- **24h** (00:39 UTC): exit 0, `healthy=true`, `empty_success=0`,
  full_sync `failed=216` → `failure_percent=2.4` (janela ainda
  majoritariamente pré-rollout; baseline 2.6), reasons
  `timeout=205, invalid_payload=11`.

## Observação da correção (§6.3.2) — primeiras horas

- **Fail-fast ao vivo** (~70min pós-swap): 22 runs failed no período —
  19 `invalid_payload` + 3 `timeout`; **19/19 `invalid_payload` com
  `FinalRunFailure.attempts_exhausted=1`** (antes da correção: 3 tentativas
  cada, sem contar requeues inter-ciclos da coorte).
- **Caracterização comparativa** (7d; baseline 22:16 UTC → 00:40 UTC):
  - coorte `patients 24 → 21`, `failed_runs 489 → 437` (52 runs saíram);
  - **`attempts_median 16.5 → 11`** (máx 112 = histórico da janela);
  - `timeout` na coorte `423 → 372`;
  - **`evolution_extraction` p90 `120.0s → 52.3s`** — extrações longas
    antes estouradas pelo orçamento fixo agora completam dentro do
    orçamento escalado (FX-S2);
  - `terminal_failing_stages: evolution_extraction=437` (100%, sem mudança
    de estágio).
- Interpretação: primeiras ~2h15 de rollout; tendências consistentes com o
  projetado (queima reduzida, cicatrização de timeouts por volume). A
  consolidação exige a janela completa de 7d e é objeto de monitoração
  contínua pelo canário §6.1.2.

## Canário §6.1.2 — timer systemd instalado (item B, autorizado)

- `/etc/systemd/system/sirhosp-ingestion-health.service` (oneshot;
  `ExecStart=/usr/bin/docker compose --env-file .env -f compose.hospital.yml
  exec -T web uv run --no-sync python manage.py check_ingestion_pipeline_health
  --max-movement-age-hours 12 --max-event-age-hours 24`;
  `TimeoutStartSec=900`; WorkingDirectory `/srv/apps/prisma`) e
  `…health.timer` (`OnCalendar=hourly`, `Persistent=true`,
  `WantedBy=timers.target`).
- `daemon-reload`, `enable --now` OK; `list-timers` confirma agendamento
  horário.
- Execução manual de validação: exit 0, saída agregada completa no journald
  (`healthy=true … failure_percent=2.4`), ~7s.
- Exit 1 do health check é o sinal de alarme (journald/monitoramento
  existente). **Sem provider de alerta** (fora do escopo, por decisão).

## Decisões do operador registradas

- **Worker clássico (`process_ingestion_runs`): sem alinhamento de
  orçamento** — permanece fora da topologia de produção; a direção acordada
  é a **remoção futura** (motivo: custo de processamento da sessão
  não-persistente, razão original da migração para o worker persistente).
  O backlog de alinhamento registrado no design D2 do change FX fica
  **cancelado** por esta decisão. O fail-fast (FX-S1) já cobre o clássico
  por ser política compartilhada de requeue.
- Arquivamento do change FX e rollout rc.15 autorizados explicitamente;
  instalação do timer §6.1.2 autorizada explicitamente.

## Rollback preservado

`compose.hospital.yml.rc14` + backup
`backups/sirhosp-20260829T223006Z.dump` (com checksum) no servidor.
Rollback = redeploy rc.14 com drenagem prévia (runbook §Rollback). Runs
fail-fast da rc.15 permanecem válidos sob a rc.14.

## Checklist final

- [x] tag/workflow/release/imagem imutáveis e conferidos (revision = commit)
- [x] assets validados por SHA-256 no servidor
- [x] drenagem total antes do swap; backup verificado
- [x] `No migrations to apply` ×2
- [x] topologia 10 workers + health/beds/imagem exata/check/structural=0
- [x] health 2h e 24h exit 0, `empty_success=0`
- [x] baseline §6.3.2 pré-rollout + primeira observação pós (fail-fast
      19/19; mediana 16.5→11; p90 120→52.3)
- [x] timer §6.1.2 instalado e validado (exit 0, journald)
- [x] nenhum `--apply`/recovery/mutação de runs/catálogo executado
- [x] decisão do operador sobre o worker clássico registrada
