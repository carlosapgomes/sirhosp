# Release Evidence Pack — v0.1.0-rc.17 (2026-09-01)

## Publicação imutável

- Tag `v0.1.0-rc.17` → commit `285c16e69ee2d5886cb03ae0d5180bbf6b4c7253`
  (release da RC17), pushed; contém os slices PFIF-S1..S5 (incluindo a
  reprovação registrada do S2 e a correção S2R), a higiene de specs
  (`fa56f0f`) e o arquivamento (`03de143`) como ancestrais.
- Workflow `Publish Release Image` (run `33512447501`, 2026-09-01T13:17Z):
  sucesso.
- Release GitHub: `isDraft=false`, `isPrerelease=true`.
- Assets: `compose.hospital.yml` (SHA-256
  `0ee94843609e21715c49a22e27e82ddfde3caeaa99b49ccbb03571690d6ae923`,
  inalterado desde a RC10) e `v0.1.0-rc.17-upgrade.md` (SHA-256
  `3de0bef7d22c6b7c6aab652940f15023b54b59f96be1f7aa67c759ec13158180`).
- Imagem GHCR `ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.17`:
  digest `sha256:32de7d4e903be133034094518cd36cb35f428b38721572adc47d0528fb2b6796`;
  label `org.opencontainers.image.revision` =
  `285c16e69ee2d5886cb03ae0d5180bbf6b4c7253` (verificado no servidor em
  2026-09-02; igual ao commit da tag).

## Gates pré-release (estação, tag commit)

- `check` sem issues; `lint` OK; `typecheck` 360 arquivos sem issues;
  `markdown-lint.sh` 0 erros;
- `unit`: **3187 passed** (baseline rc.16 = 3104; +83);
  `integration`: **474 passed** (baseline rc.16 = 391; +83);
- verificação independente dos cinco slices com RED reproduzido e gates
  re-executados (36/36 tarefas do change; relatórios
  `/tmp/sirhosp-slice-PFIF-S*-report.md` e verificação final);
- `openspec validate --specs --strict` 47/47; nenhum change ativo na tag.

## Deploy (2026-09-01, servidor `eon`)

- Preflight: `.env` modo 600 com `SIRHOSP_VERSION=v0.1.0-rc.16`;
  Compose `config --quiet` OK; dez workers rc.16; `pg_isready` OK;
  portal `{"ok": true}`; baseline canário §6.1.4 registrado antes da
  Fase A.
- Canário §6.1.4 (um worker rc.17, ciclo completo de censo): primeiro
  ciclo com `recognized_recent_encounter=6`; critérios de avanço
  atendidos (desvio operacional documentado na execução); sem
  reclassificação/requeue.
- Drenagem e backup (somente DB ativo):
  `/srv/apps/prisma/backups/sirhosp-20260901T155300Z.dump`
  (612.343.127 bytes, modo 600; SHA-256 em `.sha256` adjacente,
  verificado).
- Swap: `compose.hospital.yml.rc16` preservado (rollback);
  `.env` → `SIRHOSP_VERSION=v0.1.0-rc.17`; migrate executado duas vezes:
  `No migrations to apply` em ambas (nenhuma migration nesta release).
- Topologia pós-swap: 100% rc.17 — `web` (healthy), `census_orchestrator`,
  `summary_worker`, dez réplicas `persistent_worker`; imagem exata e
  revisão confirmadas; `manage.py check` limpo.

## Health check 24 h pós-deploy (2026-09-02T01:45Z, registrado)

```text
ingestion pipeline health: healthy=true window_hours=24
batch_invariants: empty_success=0 missing_full_sync=0 duplicate_demographics=0 recognized_recent_encounter=39
queue: active=0 oldest_age_minutes=0
full_sync: terminal=11954 succeeded=11733 failed=221 events_created=4973 failure_percent=1.8
full_sync_failure_reasons: invalid_payload=46,timeout=175
freshness: movement_present=true movement_age_minutes=95 admission_present=true admission_age_minutes=12 event_present=true event_age_minutes=15
```

- `empty_success=0` com `recognized_recent_encounter=39` em 24 h: o
  reconhecimento opera e os runs reconhecidos não contam como anomalia
  (critério central da release).
- Fila drenada (`active=0`); taxa de falha de full_sync estável (1,8%,
  igual ao baseline rc.16); frescor presente nos três eixos.
- Erros estruturais em 24 h (logs `web`/workers/orquestrador):
  `structural_errors=0`.
- Timer systemd `sirhosp-ingestion-health.timer` (§6.1.2) ativo
  (próxima execução 23:00 -03; última há 46 min).
- Serviços `Up 10 hours` coerentes com o deploy de 2026-09-01 ~15:45Z;
  sem reinícios inesperados.

## Observação de superfícies (agregados, foto 2026-09-02 01:46Z)

- Coorte do censo: 649 leitos ocupados; achados de fluxo projetados:
  `recent_encounter_without_admission=7`; demais códigos = 0 na foto.
- Levantamento Face B (5 registros revisados manualmente em 2026-09-01):
  4 saíram do censo (achados auto-resolventes por construção — sem
  persistência) e 1 permanece sem achado (espelho sincronizado/atividade
  recente). Nenhuma ação de dados foi executada; o comportamento confirma
  o desenho on-the-fly do classificador.
- Diagnóstico do badge "Sincronizado" (investigação 2026-09-01, sem
  mudança nesta release): o sinal por-run do topbar executa agregado sem
  índice em `ingestion_ingestionrun` (1,18 M de linhas, 141,8 ms por
  render); alternativa por foto de censo indexada: 0,163 ms. Tratado na
  release seguinte (change `topbar-census-freshness`).

## Encerramento

- Nenhum `--apply`, recovery, mutação/reclassificação de runs, requeue ou
  backfill executado em nenhuma fase.
- Nenhum PHI/credencial exposto nos registros desta release; todas as
  verificações com contagens agregadas.
- Decisão de topologia mantida: worker clássico fora da produção; dez
  réplicas do worker persistente.
- Próxima release candidata: RC18 (changes MSA + TCF + CFL arquivados em
  2026-09-01/02).
