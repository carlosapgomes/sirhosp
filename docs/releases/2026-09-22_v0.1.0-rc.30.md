# Release Evidence Pack — v0.1.0-rc.30 (2026-09-22)

Upgrade de produção do change `rename-enf-2b-card-display-name` (3 slices
verticais RCD-S1/RCD-S2/RCD-S3): catálogo de capacidade **v6** que renomeia
o `display_name` do grupo `ENF-2B-CARD` de `Cardioclinica / Enfermaria 2B
Cardio` para `Enfermaria 2B Cardio` (única mudança semântica vs v5, além do
`source_reference` próprio; schema `3.0`, algoritmo `occupancy-v5`,
capacidade 15 e aliases `719`/`2156` preservados; catálogos históricos
byte-preserved). Release data-only (JSON + testes), sem migrations e **sem
nenhuma mudança em código de produção** — a renomeação em telas decorre
exclusivamente do catálogo publicado. Executado pelo agente (Pi) sob
supervisão direta do operador, na pane 9 da sessão tmux (root@eon,
`/srv/apps/prisma`), seguindo o runbook `v0.1.0-rc.30-upgrade.md` (asset
imutável da release), incluindo a seção nova "Publicação do catálogo v6".

## Revisão independente e emendas dos slices

- Implementação slice a slice por worker subordinado (contexto fresco) com
  loop parent-controlled; revisão por reviewer independente (read-only,
  contexto fresco): RCD-S1 **`OK with notes` em 1 rodada** (zero achados);
  RCD-S2 **`BLOCK` (1×P1) → `OK with notes` em 2 rodadas**; RCD-S3
  **`OK` em 1 rodada** (zero achados).
- Emenda A (autorizada em loop, registrada no `tasks.md` §7 do change):
  critério de formatação emendado para "zero drift incremental vs BASE_REF"
  nos 2 arquivos de teste com drift pré-existente de formatter (precedente
  Emenda B da RC29) — provas 50/50 hunks no BASE_REF `765c186`, nenhum hunk
  nas adições; `ruff check` exit 0.

## Publicação imutável

- Tag `v0.1.0-rc.30` → commit `8613e3d` (`chore(release): prepare
  v0.1.0-rc.30 capacity catalog v6 rename`). Intervalo `v0.1.0-rc.29..rc.30`:
  `765c186` (evidence pack rc.29), `0daecb3` (feat: catálogo v6 + 9 testes
  de contrato), `94d4b1a` (test: caracterização `/beds`), `8613e3d`
  (preparação).
- Workflow `Publish Release Image` (run `35792791404`): sucesso;
  `prerelease=true`, 9/9 assets (compose hospital, scheduler, 6
  units/timers, runbook).
- Imagem GHCR `ghcr.io/carlosapgomes/sirhosp:v0.1.0-rc.30`: digest
  `sha256:370e87d594ac46401f0b4a6d3f568d722bd0e352701d8e5b892ae2a90eeb6049`
  (conferido no servidor após o pull); todos os containers de aplicação
  rodando a tag exata `v0.1.0-rc.30`.
- Compose inalterado no intervalo: SHA-256
  `a0076bed1b0046d430e88426977d7ea825d7e880d233faf088d6678da43123cd`
  idêntico ao da RC28/RC29.

## Validação pré-release (commits de implementação)

- Baseline no BASE_REF `765c186` (árvore limpa): unit **3591**.
- RCD-S1: RED real (`FileNotFoundError`) → GREEN (9 testes novos de
  contrato do v6; arquivo completo 158 passed). RCD-S2: caracterização com
  controle de mutação (baseline verde → ordem invertida vermelha → reversão
  verde; arquivo completo 113 passed). RCD-S3: dry-run local em container de
  teste com data futura distante — exit 0, `occupancy-v5`, 43/48/47,
  39 standard, 4 unrated, 666/666, nada persistido (`0 → 0`).
- Finais: unit **3601** (= 3591 + 9 + 1); integration **701** (= RC29);
  `quality-gate` exit 0 (Django check, unit, Ruff, mypy — "Success: no
  issues found in 416 source files"); `openspec validate --strict` válido;
  `markdown-lint` 0 erros (786 arquivos); scan de PHI no diff limpo
  (`765c186..HEAD`: 3 arquivos, +847/−0).

## Baseline pré-deploy (rc.29)

- Topologia: orquestrador + 10 persistent workers + summary + web
  `(healthy)` + db postgres `(healthy, 24 h)`. 14/14 containers up.
- `.env` modo 600 com `SIRHOSP_VERSION=v0.1.0-rc.29`.

## Deploy (fases do runbook, todas com critério verificado)

1. **Pausa**: `sirhosp-stale-reconciliation.timer` → stopped;
   `census_orchestrator` → stopped.
2. **Drenagem**: fila inicial `655` (644 queued + 6 running) + 1 batch →
   `0 0` em ~19 min (workers mantidos ativos, ~25 runs/min, backlog jovem
   de 0,1–0,7 h todo `full_sync`, 422 concluídos nos 15 min iniciais;
   nenhum container reiniciado; polling agregado, sem PHI).
3. **Assets e backup**: runbook (14.361 bytes) + compose (6.863 bytes)
   baixados da release imutável; backup
   `backups/sirhosp-20260922T230700Z.dump` — 899.998.675 bytes, catálogo
   validado com `pg_restore --list`.
4. **Upgrade**: `.env` → `v0.1.0-rc.30` (modo 600 preservado); swap do
   Compose (rollback preservado em `compose.hospital.yml.v0.1.0-rc.29`);
   `config --quiet`; `pull`; `migrate` → **`No migrations to apply.`**
   (como documentado); `up -d` com `--scale persistent_worker=10`.
5. **Validação**: 14/14 containers; web `(healthy)`; todos na imagem
   `v0.1.0-rc.30`; `manage.py check` 0 issues; `/health/` ok; anônimo →
   302 em `painel/`, `painel/altas/`, `altas/`, `reconciliacao/`, `censo/`
   **e `beds/`**; logs sem error/traceback/exception.
6. **Publicação do catálogo v6** (seção nova do runbook): SHA-256 do JSON
   na imagem conferido = `3f7a02ea3637a09e4a4b58c4c1139e44b0302d75b6b04555cfb6e30b70d8cabb`;
   dry-run com `--effective-from 2026-09-23` → `validado (dry-run)`,
   `occupancy-v5`, 43/48/47, 39/39/4, 666/666; publicação → linha nova
   única `2026-09-23 3f7a02ea3637 occupancy-v5 groups=43` (6 versões
   totais, histórico preservado); retry → `já publicado (idempotente)`.
7. **Retomada**: `census_orchestrator` Up (recovery intraday
   `Succeeded: 1 / Failed: 0`, 27 s); timer active; ciclo único em curso e
   workers processando (6 runs ativos no momento da observação); 0 erros
   nos logs.

## Incidentes e desvios

- **Sessão root expirou no meio do runbook** (inatividade durante o watch
  do CI do GitHub, ~13 min; provável `TMOUT`): nenhuma etapa executada no
  estado degradado; operador re-autenticou (`su -`); variáveis de ambiente
  do runbook reexportadas antes de prosseguir. Sem impacto.
- **Saída da publicação truncada por `head -3`** (BrokenPipe após o commit
  da transação): estado verificado diretamente no banco antes de prosseguir
  (linha v6 presente, exatamente 1) e idempotência confirmada na sequência;
  `grep -E` na retry capturou a mensagem completa. Sem impacto.
- Dois erros de diagnóstico read-only do agente (campos `created_at` →
  `queued_at`; `document_sha256` → `source_sha256`): FieldErrors sem efeito
  colateral, corrigidos na consulta seguinte.
- Drenagem de ~19 min reflete burst `full_sync` pré-existente (655 runs),
  não defeito; nenhuma janela de ciclo foi forçada.

## Encerramento

- Rollback (code-only): redeploy da RC29 preservada
  (`compose.hospital.yml.v0.1.0-rc.29` + `.env`); sem migrations, o backup é
  salvaguarda e não deve ser restaurado em rollback de imagem. Nota v6: o
  rollback de imagem **não** remove o catálogo publicado (imutável, sem
  unpublish); a v6 permanece inerte até a vigência e é compatível com a
  RC29 (schema/algoritmo idênticos; reversão do rename = v7 futura).
- Pendências conscientes: **verificação pós-vigência amanhã
  (2026-09-23)** — após a primeira medição/materialização do dia local,
  conferir `/beds` exibindo `Enfermaria 2B Cardio` junto dos pares
  `Enfermaria 2…`, rótulo composto ausente no dia vigente e preservado em
  dias anteriores (tasks.md §5.2 do change); smoke funcional autenticado
  manual de `/beds/`; monitoração padrão de 24 h.
- Nenhum backfill, reprocessamento ou mutação de dados históricos foi
  executado; catálogos v1–v5 permanecem byte-preserved (ADR-0003).

## Rastreabilidade

- Change: [openspec/changes/rename-enf-2b-card-display-name](../openspec/changes/rename-enf-2b-card-display-name)
  (arquivamento pendente da verificação pós-vigência §5.2).
- Specs deltas: `versioned-sector-capacity-catalog` e
  `bed-status-capacity-view` (no change).
- Runbook: `docs/releases/v0.1.0-rc.30-upgrade.md` (asset da release).
- Commits: `0daecb3` (feat) · `94d4b1a` (test) · `8613e3d` (tag).
- Versão anterior: `v0.1.0-rc.29` (`4a3d890`).
